# GitHub OAuth Configuration
# Get these by creating a GitHub OAuth App at: https://github.com/settings/developers

# Your GitHub OAuth App Client ID
EXPO_PUBLIC_GITHUB_CLIENT_ID=your_client_id_here

# Your GitHub OAuth App Client Secret (keep this secret!)
# Note: For production, handle this via a serverless function
GITHUB_CLIENT_SECRET=your_client_secret_here

# Redirect URI (must match your GitHub OAuth App settings)
EXPO_PUBLIC_GITHUB_REDIRECT_URI=http://localhost:8081/auth/callback
